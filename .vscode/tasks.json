{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Configure CMake",
            "type": "shell",
            "command": "cmake",
            "args": [
                "-G", "Unix Makefiles",
                "-S", ".",
                "-B", "build",
                "-DBUILD_TESTS=ON",  
                "-DCMAKE_BUILD_TYPE=Debug"
            ],
            "icon": { "id": "tools" },
        },
        {
            "label": "Compile Unit Tests",
            "type": "shell",
            "command": "cmake",
            "args": [
                "--build", "build",
                "--verbose"
            ],
            "icon": { "id": "gear" },
        }
    ]
}